import { Routes } from '@angular/router';
import { PositionTree } from './components/position-tree/position-tree.component';

export const routes: Routes = [
    {path: '', component: PositionTree}
];
